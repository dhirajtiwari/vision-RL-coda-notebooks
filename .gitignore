
# Build artifacts
# Coverage / eval artifacts
# Environment
# Generated docs / diagrams
# IDE / OS
# Ignore only the root-level docs lockfile; the frontend lockfile MUST be
# Kubernetes secrets (use secrets.yaml.example)
# Node
# Python
# Runtime data (optional — keep fixtures, ignore volatile runtime)
# Volatile ETL run outputs (timestamped) — generated by the pipeline/tests, not fixtures
# committed so CI's `npm ci` (and the setup-node cache) works.
*.egg-info/
*.log
*.py[cod]
*.swp
.DS_Store
.coverage
.env
.env.local
.idea/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.vscode/
/package-lock.json
__pycache__/
coverage.xml
data/*.db
data/enterprise_sources/claims_submissions.json
data/escalations.json
data/lineage/
data/pipeline_staging/run-*.json
data/simulated_cases.json
data/study_progress/
data/study_review/
docs/graphviz/rendered/
eval-report.json
htmlcov/
k8s/**/secrets.yaml
node_modules/
venv/
