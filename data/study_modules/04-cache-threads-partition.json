{
  "id": "04-cache-threads-partition",
  "title": "Caching, multi-threading, partitioning",
  "description": "Three-tier cache thinking, thread pools for I/O, logical partitions, invalidation.",
  "tags": [
    "cache",
    "redis",
    "threading",
    "partitioning",
    "performance"
  ],
  "story": "One cashier: a notepad of recent answers is enough. Ten cashiers: they need a shared whiteboard (Redis) or they contradict each other. Writes still go through a careful manager (primary Neo4j); reads can scale out. Threads help when waiting on many connectors — not when holding the GIL on pure CPU.",
  "one_liner": "Cache hot diagnose keys; parameterize Cypher; bound thread pools; partition by tenant|product.",
  "change_table": [],
  "beats": [
    {
      "id": "runtime-patterns",
      "title": "Cache + threads + partitions (patterns)",
      "language": "python",
      "narrative": "Memorize key composition, TTL, invalidation after promote.",
      "code": "from functools import lru_cache\nfrom runtime.cache import get_named_cache\nfrom runtime.partitioning import partition_key\nimport concurrent.futures\n\n# Tier 1 — application cache (memory or Redis)\ncache = get_named_cache(\"diagnose\", ttl_seconds=90)\n\ndef cached_diagnose(product_id: str, symptom_key: str, compute):\n    key = f\"diag:{product_id}:{symptom_key}\"  # include all inputs that change the answer\n    hit = cache.get(key)\n    if hit is not None:\n        return hit\n    value = compute()\n    cache.set(key, value)\n    return value\n\n# Tier 2 — parameterized Cypher so Neo4j caches query plans\nGOOD = \"MATCH (p:Product {product_id: $id}) RETURN p\"\n# BAD  = f\"MATCH (p:Product {{product_id: '{id}'}}) RETURN p\"\n\n# Concurrency — bound worker pool for I/O fan-out (connectors)\ndef fetch_sources_parallel(fns: list):\n    with concurrent.futures.ThreadPoolExecutor(max_workers=4) as ex:\n        return list(ex.map(lambda f: f(), fns))\n\n# Partitioning — logical keys today; physical shards later\ndef rate_limit_key(tenant: str, client: str, route: str) -> str:\n    return partition_key(tenant, client, route)  # e.g. tenant|client|route\n\n# Invalidation — after promote/ETL\ndef on_graph_loaded():\n    cache.clear()  # or invalidate_all_named_caches()",
      "annotations": [
        {
          "line": 6,
          "note": "Named cache with TTL — memory or Redis backend."
        },
        {
          "line": 9,
          "note": "Key must include every input that changes the result."
        },
        {
          "line": 18,
          "note": "Parameterized Cypher = plan reuse + safety."
        },
        {
          "line": 23,
          "note": "ThreadPool for I/O-bound connector fan-out."
        },
        {
          "line": 29,
          "note": "Logical partition key for multi-tenant limits."
        },
        {
          "line": 33,
          "note": "Invalidate on graph load or users see stale truth."
        }
      ],
      "line_quiz": [
        {
          "line": 9,
          "prompt": "What belongs in a diagnose cache key?",
          "answer": "product_id + symptoms (+ tenant)",
          "choices": [
            "product_id + symptoms (+ tenant)",
            "Only the wall clock time",
            "Random UUID always",
            "Just the word diagnose"
          ],
          "why": ""
        },
        {
          "line": 33,
          "prompt": "When clear cache?",
          "answer": "After ETL/promote graph load",
          "choices": [
            "After ETL/promote graph load",
            "Never",
            "Only on Fridays",
            "Before every Cypher keyword"
          ],
          "why": ""
        }
      ],
      "fill_blanks": {
        "template": "key = f\"diag:{{k1}}:{{k2}}\"\nhit = cache.{{k3}}(key)",
        "blanks": [
          {
            "id": "k1",
            "answer": "{product_id}",
            "hint": ""
          },
          {
            "id": "k2",
            "answer": "{symptom_key}",
            "hint": ""
          },
          {
            "id": "k3",
            "answer": "get",
            "hint": ""
          }
        ]
      }
    }
  ],
  "concepts": [
    {
      "term": "TTL cache",
      "definition": "Entries expire after seconds — freshness vs speed",
      "analogy": ""
    },
    {
      "term": "Redis shared state",
      "definition": "Multi-pod cache/rate/admission",
      "analogy": ""
    },
    {
      "term": "Logical partition",
      "definition": "Key design before physical shards",
      "analogy": ""
    },
    {
      "term": "CAP for diagnosis",
      "definition": "Prefer consistency of knowledge over availability",
      "analogy": ""
    }
  ],
  "self_quiz": [
    {
      "q": "Is Redis required for a single-node demo?",
      "a": "No — memory backends work; Redis for multi-replica.",
      "difficulty": "medium"
    },
    {
      "q": "Why not unbounded threads?",
      "a": "Connection storms and tail latency; bound max_workers.",
      "difficulty": "medium"
    },
    {
      "q": "Product-based sharding idea?",
      "a": "Partition graph data by product line when scale forces Fabric.",
      "difficulty": "medium"
    }
  ],
  "common_mistakes": [
    "Caching without invalidation after promote.",
    "String-built Cypher killing the plan cache.",
    "Claiming Fabric/replicas are as-built when only logical keys exist."
  ],
  "final_boss": [
    "Design diagnose cache keys for multi-tenant API.",
    "Explain memory vs Redis for rate limits.",
    "Draw CAP choice for warranty knowledge updates."
  ],
  "source": "seed",
  "order": 40,
  "estimated_minutes": 30
}
