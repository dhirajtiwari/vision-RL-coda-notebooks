apiVersion: v1
kind: Service
metadata:
  name: diagnostics-api
  labels:
    app: diagnostics-api
spec:
  selector:
    app: diagnostics-api
  ports:
    - name: http           # named port so the ServiceMonitor can target it
      port: 8080
      targetPort: 8080
