pydantic>=2.0
pydantic-settings>=2.0
pandas>=2.0
neo4j>=5.0
langgraph>=0.2
streamlit>=1.30
watchdog>=4.0
python-dotenv>=1.0
langchain>=0.3
langchain-community>=0.3
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
pyvis>=0.3.2
# Optional multi-replica shared state (cache, rate limit, budget). Empty REDIS_URL
# keeps the demo on in-process memory; install redis client always so prod can enable.
redis>=5.0
# --- LLMOps: observability + config (lightweight, always installed) ---
prometheus-client>=0.20
PyYAML>=6.0
jsonschema>=4.0
# --- Optional OpenTelemetry stack: `pip install -r requirements-observability.txt`
#     (kept out of the core install so the local demo stays lean)
